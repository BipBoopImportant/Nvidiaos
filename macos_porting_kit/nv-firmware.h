// This is a dummy header file to satisfy the #include <nv-firmware.h>
// directive in the NVIDIA source code. This file is required because the
// original is either part of the Linux kernel headers or generated during
// the Linux build process.
//
// As you proceed with the port, you may need to add type definitions or
// other declarations here to satisfy the compiler.
#ifndef NV_FIRMWARE_H
#define NV_FIRMWARE_H

#endif // NV_FIRMWARE_H